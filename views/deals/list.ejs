<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>🔥 Active Deals</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      min-height: 100vh;
      background: linear-gradient(135deg, #d0f0c0, #a8e6cf);
      animation: bgMove 30s ease infinite;
    }
    @keyframes bgMove {
      0% { background-position: 0% 0%; }
      50% { background-position: 100% 100%; }
      100% { background-position: 0% 0%; }
    }
    h2 {
      text-align: center;
      font-weight: 700;
      color: #1b5e20;
      margin-bottom: 40px;
      text-shadow: 1px 1px 3px rgba(0,0,0,0.2);
      font-size: 2rem;
    }

    .carousel-item > .row {
      display: flex;
      flex-wrap: nowrap;
      gap: 20px;
      justify-content: center;
    }
    .carousel-item .col {
      flex: 1 0 0%;
    }
    @media (max-width: 992px) { .carousel-item .col { flex: 0 0 48%; } }
    @media (max-width: 576px) { .carousel-item .col { flex: 0 0 95%; } }

    .card {
      border-radius: 20px;
      background: #ffffffee;
      overflow: hidden;
      transition: transform 0.5s, box-shadow 0.5s;
      position: relative;
    }
    .card:hover {
      transform: translateY(-10px) scale(1.05);
      box-shadow: 0 20px 40px rgba(0,0,0,0.25);
    }
    .card-img-top {
      height: 250px;
      object-fit: cover;
      transition: transform 0.5s ease;
    }
    .card:hover .card-img-top {
      transform: scale(1.08) rotateZ(1deg);
    }

    .badge-featured {
      position: absolute;
      top: 10px;
      left: 10px;
      background: #aed581;
      color: #1b5e20;
      font-weight: 700;
      padding: 6px 14px;
      border-radius: 50px;
      box-shadow: 0 0 12px rgba(0,0,0,0.2);
      animation: glow 1.5s infinite alternate;
      z-index: 2;
    }
    @keyframes glow {
      from { box-shadow: 0 0 8px #aed581; }
      to { box-shadow: 0 0 20px #7cb342; }
    }

    .price {
      font-weight: 700;
      font-size: 1.2rem;
      color: #2e7d32;
      margin-bottom: 10px;
    }

    .btn-buy {
      background: linear-gradient(45deg, #43a047, #66bb6a);
      color: #fff;
      border-radius: 50px;
      font-weight: 600;
      transition: all 0.3s;
    }
    .btn-buy:hover {
      background: linear-gradient(45deg, #2e7d32, #388e3c);
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.3);
    }

    .btn-secondary, .btn-primary { border-radius: 50px; width: 100%; }

    /* Carousel control colors */
    .carousel-control-prev-icon,
    .carousel-control-next-icon {
      filter: invert(25%) sepia(100%) saturate(500%) hue-rotate(90deg);
    }
  </style>
</head>
<body class="container mt-5">

<h2>🔥 Active Deals</h2>

<% if (deals.length > 0) { %>
<div id="dealsCarousel" class="carousel slide" data-bs-ride="carousel" data-bs-interval="4000">
  <div class="carousel-inner">
    <% for (let i = 0; i < deals.length; i += 3) { %>
      <div class="carousel-item <%= i === 0 ? 'active' : '' %>">
        <div class="row">
          <% deals.slice(i, i + 3).forEach(deal => { %>
            <div class="col mb-4">
              <div class="card shadow-sm h-100 position-relative">
                <% if (deal.featured) { %>
                  <span class="badge-featured">🌟 Featured</span>
                <% } %>
                <% if (deal.image) { %>
                  <img src="<%= deal.image %>" class="card-img-top" alt="Deal Image">
                <% } %>
                <div class="card-body d-flex flex-column">
                  <h5 class="card-title"><%= deal.title %></h5>
                  <p class="card-text"><%= deal.description %></p>
                  <p class="price">₹<%= deal.price %></p>
                  <p class="text-danger small">Ends at: <%= deal.endTime.toLocaleString() %></p>
                  <div class="mt-auto">
                    <% if (user && deal.claimedBy.some(id => id.toString() === user._id.toString())) { %>
                      <button class="btn btn-secondary" disabled>Already Purchased</button>
                    <% } else if (!user) { %>
                      <a href="/login" class="btn btn-primary">Login to Buy</a>
                    <% } else { %>
                      <form action="/api/deals/<%= deal._id %>/claim" method="POST">
                        <button type="submit" class="btn btn-buy">Buy Now</button>
                      </form>
                    <% } %>
                  </div>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
    <% } %>
  </div>

  <button class="carousel-control-prev" type="button" data-bs-target="#dealsCarousel" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#dealsCarousel" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>
<% } else { %>
<p class="text-center fs-5">😔 No active deals right now.</p>
<% } %>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
